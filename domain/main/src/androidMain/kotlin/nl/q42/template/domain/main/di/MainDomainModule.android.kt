package nl.q42.template.domain.main.di

import org.koin.core.module.Module
import org.koin.dsl.module

actual val domainPlatformModule: Module = module {
    // Add platform-specific dependencies here, if needed
}